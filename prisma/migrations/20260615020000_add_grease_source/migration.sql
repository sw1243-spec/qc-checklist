BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[GreaseLog] ADD [source] NVARCHAR(1000) NOT NULL CONSTRAINT [GreaseLog_source_df] DEFAULT 'manual';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

